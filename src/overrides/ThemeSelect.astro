---
/**
 * Empty component — removes the theme toggle from the header.
 * Starlight renders ThemeSelect in the header bar by default.
 * Replacing it with an empty component hides the toggle cleanly.
 */
---
